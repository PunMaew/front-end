version : '3'
services :
  server :
    env_file:
      - .env
    build: 
      context: .
    image: frontend:latest
    ports:
      - "8080:80"
      
networks:
  default:
    external:
      name: punmaew

